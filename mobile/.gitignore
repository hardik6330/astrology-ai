node_modules/
.expo/
dist/
web-build/
.env
*.log
.DS_Store
# Prebuild output at project root only — anchored so the local native module
# (modules/hand-landmarker/android|ios) stays tracked and ships to EAS.
/ios/
/android/
